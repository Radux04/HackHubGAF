# Porta dell'app (se vuoi cambiarla)
server.port=8080

# ========== H2 IN MEMORY DB ==========
spring.datasource.url=jdbc:h2:mem:hackhubdb
spring.datasource.driver-class-name=org.h2.Driver
spring.datasource.username=sa
spring.datasource.password=

# Mostra la console web di H2
spring.h2.console.enabled=true
spring.h2.console.path=/h2-console

# ========== JPA / HIBERNATE ==========
# Crea/droppa lo schema ad ogni avvio (per sviluppo va benissimo)
spring.jpa.hibernate.ddl-auto=create-drop

# Mostra le query SQL in console
spring.jpa.show-sql=true

# Format SQL leggibile
spring.jpa.properties.hibernate.format_sql=true
